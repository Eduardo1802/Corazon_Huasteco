name: Prueba Renderizado Sobre Nosotros
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código fuente
      uses: actions/checkout@v2

    - name: Instalar dependencias
      run: npm install

    - name: Ejecutar pruebas TestCafe
      run: testcafe chromium:headless ./tests/test_renderizado_sobre_nosotros.js
